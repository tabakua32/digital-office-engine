---
name: vibecoder-v2
description: >
  Командна матриця, воркфлови та найкращі практики Claude Code для vibe coding.
  Активуй коли: користувач запитує команди Claude Code, налаштування CLAUDE.md,
  воркфлови кодування, hooks, skills, subagents, MCP, оптимізацію контексту або
  вартості, починає новий проєкт, питає "як правильно використовувати Claude Code",
  або потребує recovery після зламаної сесії.
---

# Claude Code Vibe — Командна матриця та воркфлови

<!-- ═══ PRIMACY ZONE — перші 20% ═══ -->

## Критичні правила (порушення = провал сесії)

1. **Plan Mode перед кодом** — `Shift+Tab` двічі. Завжди. Без винятків для "простих" задач.
2. **`/clear` між задачами** — контекст деградує мовчки після 70% заповнення.
3. **Commit після кожної підзадачі** — не в кінці. Checkpoint до ризикових змін.
4. **CLAUDE.md ≤ 150 рядків** — кожна зайва інструкція рівномірно знижує дотримання всіх.
5. **Не редагуй файли вручну під час сесії** — ламає prompt cache → витрати ×10.
6. **≤ 5 MCP серверів** — 20k+ токенів MCPs = залишається 20k для роботи.
7. **`rm -rf` заборонено в deny-rules** — задокументовані кейси знищення даних. Деталі: [security.md](references/security.md).
8. **Верифікація результату** — завжди давай Claude спосіб підтвердити роботу: тести, typecheck, diff.

Сесія зламалась → [recovery.md](references/recovery.md).

---

## MECE Командна матриця

### Блок A — Термінальні команди

| Команда | Дія | Коли |
|---|---|---|
| `claude` | Інтерактивна сесія | Старт роботи |
| `claude "запит"` | Одноразова задача | Швидке питання |
| `claude -p "запит"` | Headless / pipeline | Автоматизація, CI/CD |
| `cat file.txt \| claude` | Аналіз файлу | Огляд коду/даних |
| `claude --output-format stream-json` | JSON-стрім | Пайплайн інтеграція |

### Блок B — Slash-команди (під час сесії)

| Команда | Дія | Пріоритет |
|---|---|---|
| `/init` | Збагатити CLAUDE.md з існуючого коду | 🔴 Після першого коду (не перед) |
| `/clear` | Очистити контекст | 🔴 Між задачами |
| `/context` | Показати % використання | 🔴 Моніторинг деградації |
| `/rewind` | Огляд checkpoints, повернення до стану | 🔴 Recovery після помилок |
| `/compact [фокус]` | Стиснути контекст з інструкцією | 🟡 При 65–70%, з явним фокусом |
| `/memory` | Переглянути auto-memory | 🟡 Аудит пам'яті |
| `/hooks` | Налаштувати хуки | 🟡 Автоматизація |
| `/permissions` | Контроль дозволів | 🟡 Безпека |
| `/cost` | Витрати поточної сесії | 🟡 Моніторинг |
| `/sandbox` | Ізольоване виконання | 🟡 Ризиковані операції |

### Блок C — Клавіатурні скорочення

| Скорочення | Дія | Контекст |
|---|---|---|
| `Shift+Tab` двічі | Plan Mode | 🔴 Перед будь-яким кодом |
| `Escape` | Перервати Claude | 🔴 Коли іде не туди |
| `Escape` двічі | Список попередніх повідомлень | Відкатитись назад |
| `Ctrl+B` | Запустити у фоні | Довгі задачі |
| `#` | Записати правило в CLAUDE.md зараз | Після виправлення помилки |
| `Cmd+Ctrl+Shift+4` → `Ctrl+V` | Скріншот → вставка | UI фідбек (macOS) |

### Блок D — Thinking-модифікатори

| Слово | Бюджет | Коли |
|---|---|---|
| `think` | ~4 000 токенів | Звичайний код |
| `think hard` | ~10 000 токенів | Архітектурні рішення |
| `think harder` | Більший бюджет | Складні баги |
| `ultrathink` | 31 999 токенів | Критичні рішення (v2.1.11+: вбудовано за замовчуванням) |

---

## Чотири воркфлови (від простого до складного)

### Воркфлов 0 — Greenfield (з нуля)
Повна послідовність: [greenfield.md](references/greenfield.md)

```
git init → CLAUDE.md вручну → Plan Mode → spec-interview → реалізація → commit → /init
```

---

### Воркфлов 1 — Стандартний (80% задач)

```
Explore → Plan → Code → Commit
```

1. `"Прочитай @src/auth.ts та README. Не пиши код."`
2. `Shift+Tab` двічі → Plan Mode → `"think hard: як реалізувати X"`
3. Вихід з Plan Mode → реалізація
4. `"Запусти npm run typecheck. Напиши описовий commit."`

---

### Воркфлов 2 — TDD (внутрішній фаворит Anthropic)

```
Failing tests → Commit → Реалізація → Verify
```

1. `"Напиши failing tests для [функція]. Не реалізовуй."`
2. Commit тестів окремо
3. `"Реалізуй код щоб тести проходили. Не змінюй тести."`
4. `"Prove to me this works — diff поведінку між main та цією гілкою."`

---

### Воркфлов 3 — Паралельний (максимальний throughput)

```bash
git worktree add ../project-feature-a feature-a && cd ../project-feature-a && claude
# В іншому терміналі:
git worktree add ../project-feature-b feature-b && cd ../project-feature-b && claude
```

Борис Черни (автор Claude Code) — 10–15 паралельних сесій. 10–20% кидає як невдалі. Це нормально.

---

## CLAUDE.md — battle-tested шаблон

```markdown
# [Назва проєкту]

## About
[1-2 речення: що робить і навіщо]

## Tech Stack
Language: [...] | Framework: [...] | Database: [...] | Package manager: [...]

## Commands
- Dev: `npm run dev`      - Test: `npm test`
- Typecheck: `npm run typecheck`  - Lint: `npm run lint`

## Workflow Rules
- Use Plan Mode for any non-trivial task
- Run typecheck after every code change
- Commit after each completed sub-task
- Verify imports exist — never guess API signatures
- If stuck 2+ times on same fix: stop, list failures, propose 3 alternatives

## Code Standards
- TypeScript strict, no `any` — use `unknown` or proper types
- Files under 300 lines

## Architecture References
- Auth flow: docs/authentication.md
- DB schema: docs/schema.md

## Error Prevention
[Росте з часом — після кожного виправлення додавай правило]
```

**Правило компаундингу:** після виправлення: `"Оновити CLAUDE.md щоб ця помилка не повторилась"`.
Команда Anthropic оновлює CLAUDE.md під час PR review через GitHub Action.

---

## Структура проєкту (еталонна)

```
your-project/
├── CLAUDE.md                    ← ≤150 рядків, committed
├── .mcp.json                    ← MCP сервери, committed
└── .claude/
    ├── settings.json            ← Дозволи + hooks, committed
    ├── settings.local.json      ← Особисті, gitignored
    ├── skills/                  ← Skill файли
    ├── agents/                  ← Subagent конфіги
    └── commands/                ← Slash-команди (.md)

~/.claude/
├── CLAUDE.md                    ← ≤15 рядків, глобальні
└── settings.json                ← Глобальні hooks + deny-rules
```

---

## Reference файли (завантажуються за потребою)

| Ситуація | Файл |
|---|---|
| Новий проєкт з нуля | [greenfield.md](references/greenfield.md) |
| Сесія зламалась / Claude в петлі | [recovery.md](references/recovery.md) |
| Хочу кращі промпти | [power-prompts.md](references/power-prompts.md) |
| Безпека / rm-rf / дозволи | [security.md](references/security.md) |
| Hooks та автоматизація | [hooks-workflows.md](references/hooks-workflows.md) |
| Skills та subagents | [skills-agents.md](references/skills-agents.md) |
| Оптимізація вартості | [cost-optimization.md](references/cost-optimization.md) |
| MCP стек | [mcp-stack.md](references/mcp-stack.md) |

---

<!-- ═══ RECENCY ZONE — останні 10% ═══ -->

## Критичні нагадування (повторення primacy zone)

- Plan Mode (`Shift+Tab` двічі) — перед будь-яким кодом
- `/context` — моніторь деградацію, `/clear` при 70%+
- Commit після кожної підзадачі
- CLAUDE.md ≤ 150 рядків
- Файли вручну не редагуй під час сесії
- `rm -rf` → deny-rules → [security.md](references/security.md)
- Сесія в петлі → `/rewind` → [recovery.md](references/recovery.md)
- Верифікація: тести / typecheck / diff — давай Claude завжди

## Контрольний список нового проєкту
- [ ] `git init` + ручний CLAUDE.md (≤150 рядків)
- [ ] `~/.claude/settings.json` з deny для `rm -rf` та `.env`
- [ ] `.mcp.json` з Context7 + Playwright
- [ ] Перша сесія: Plan Mode + spec-interview
- [ ] Воркфлов: Explore → Plan → Code → Commit
- [ ] Після першого виправлення: `"Оновити CLAUDE.md"`
- [ ] `/init` для збагачення CLAUDE.md (після першого коду)
