# TASK-02: Дослідження System Prompts

## МЕТА
Екстрагувати ORCHESTRATION PATTERNS з production system prompts.
NanoClaw = обгортка для Claude Agent SDK, тому патерни Claude-based систем — пряма інструкція.

## КОНТЕКСТ
Ми будуємо систему з 77 AI-агентів які працюють через Claude Agent SDK у контейнерах.
Кожен агент отримує system prompt (= його SKILL.md + CLAUDE.md).
Потрібно зрозуміти як НАЙКРАЩІ production системи структурують system prompts.

## INPUT FILES (завантаж перед стартом)

### 🔴 Пріоритет 1 — Claude-based (НАШ runtime)
```
# Claude Code system prompt — з репо leaked prompts
# Claude.ai system prompt — з репо leaked prompts
```

### 🔴 Пріоритет 2 — Multi-agent orchestrator
```
# Manus system prompt — з репо leaked prompts
```

### 🟡 Пріоритет 3 — IDE agents (tool use patterns)
```
# Cursor Agent mode — з репо leaked prompts
# Windsurf/Devin — якщо є
```

### Репо з leaked prompts:
```
git clone https://github.com/x1xhlol/system-prompts-and-models-of-ai-tools.git
git clone https://github.com/thekishandev/ai-system-prompt.git
```
Gist: https://gist.github.com/DropFan/90841333a7f3b185caa83ba9eab6c73b

### УВАГА: вибери НАЙСВІЖІШІ версії промптів (2025-2026).

## ЗАДАЧА

### Крок 1: Знайди і зафіксуй промпти
Для кожної системи знайди system prompt. Запиши:
```
SYSTEM: [назва]
SOURCE: [файл/шлях]
DATE: [дата промпту якщо відома]
SIZE: [кількість рядків / приблизна кількість токенів]
```

### Крок 2: Аналіз за 8 вимірами
Для КОЖНОГО system prompt витягни:

```
SYSTEM: [назва]

1. IDENTITY (роль, місія, boundaries):
   - Як визначена роль (одне речення? абзац? список?)
   - Чи є explicit boundaries ("ти НЕ робиш X")
   - Чи є mission statement

2. SAFETY (заборони, ескалація):
   - Які дії ЗАБОРОНЕНІ (список)
   - Чи є escalation triggers ("якщо юзер просить X → зроби Y")
   - Чи є refusal patterns (як ВІДМОВЛЯТИ)

3. TOOLS (інструменти, tool selection):
   - Як описані інструменти (inline? окремий блок? JSON schema?)
   - Чи є tool selection logic ("для задачі X використай tool Y")
   - Чи є tool chaining patterns ("спочатку A, потім B")

4. ORCHESTRATION (делегування, handoff):
   - Чи є sub-agent delegation
   - Формат передачі задачі між агентами
   - Error recovery при невдачі sub-agent
   - ТІЛЬКИ для Manus/multi-agent систем

5. OUTPUT (контроль формату):
   - Як задається формат відповіді
   - Чи є output templates/schemas
   - Чи є self-check перед відповіддю

6. MEMORY (контекст між сесіями):
   - Як працює пам'ять
   - Чи є file-based memory (як CLAUDE.md)
   - Чи є session management

7. ERROR (обробка помилок):
   - Що робити при невдачі
   - Degradation strategy
   - Retry logic

8. UNIQUE (що є ТІЛЬКИ тут):
   - Патерни яких НЕМАЄ в інших промптах
   - Інноваційні підходи
```

### Крок 3: Cross-System Pattern Matrix
Побудуй матрицю: які патерни ПОВТОРЮЮТЬСЯ у 2+ системах.

```
| PATTERN | Claude Code | Claude.ai | Manus | Cursor | Windsurf |
|---------|------------|-----------|-------|--------|----------|
| [назва] | ✅/❌      | ✅/❌     | ✅/❌ | ✅/❌  | ✅/❌    |
```

Для кожного ПОВТОРЮВАНОГО патерну:
```
PATTERN: [назва]
SYSTEMS: [де зустрічається]
IMPLEMENTATION: [як реалізовано — порівняй підходи]
BEST_VERSION: [яка реалізація найкраща і чому]
NANOCLAW_RELEVANCE: [HIGH | MEDIUM | LOW — і чому]
```

### Крок 4: Manus Deep Dive (окремо)
Manus — єдиний production multi-agent orchestrator.
Витягни ДОДАТКОВО:
- Як розбиває задачу на sub-tasks
- Формат task specification для sub-agent
- Як збирає результати від sub-agents
- Як обробляє часткові невдачі
- Яка ієрархія агентів

### Крок 5: NanoClaw-Specific Recommendations
На основі аналізу — що КОНКРЕТНО взяти для NanoClaw:
```
RECOMMENDATION: [що саме]
SOURCE: [з якого промпту]
HOW_TO_ADAPT: [як адаптувати під NanoClaw архітектуру]
PRIORITY: [🔴 | 🟡 | 🟢]
```

## OUTPUT
Один файл: `system_prompts_analysis.md`
Структура:
1. Inventory (крок 1)
2. Per-System Analysis (крок 2)
3. Cross-System Pattern Matrix (крок 3) — НАЙВАЖЛИВІШЕ
4. Manus Orchestration Deep Dive (крок 4)
5. NanoClaw Recommendations (крок 5)

## QUALITY GATES
- [ ] Мінімум 4 system prompts проаналізовано (Claude Code, Claude.ai, Manus, Cursor)
- [ ] Усі 8 вимірів заповнені для кожного промпту
- [ ] Pattern Matrix містить мінімум 20 патернів
- [ ] Manus deep dive має конкретні приклади orchestration
- [ ] Recommendations прив'язані до NanoClaw архітектури (контейнери, IPC, CLAUDE.md)
